import React from 'react'
import withLayout from '../components/withLayout';

const Features = () => {
    return (
        <div className="container">
            <h2>Features page</h2>
            <p>This is a simple Features page!</p>
        </div>
    )
}

export default withLayout(Features);
